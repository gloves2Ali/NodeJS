index.js --- 主文件 启动文件
server.js --- 服务文件
requestHandlers.js --- 请求模块文件
router.js --- 路由文件

请求处理程序->请求路由->服务器

Node.js是单线程的，尽量避免阻塞操作，取而代之，多使用
非阻塞操作。要用非阻塞操作，我们需要使用回调，通过将函数作为参数
传递给其他需要花时间做处理的函数（比方说，休眠10秒，或者查询数据库，
又或者是进行大量的计算）。

以非阻塞操作进行请求响应
nodejs实现方案：函数传递.
